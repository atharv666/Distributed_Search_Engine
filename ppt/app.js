const slides=[...document.querySelectorAll('.slide')].slice(0,16),progress=document.querySelector('#progress'),counter=document.querySelector('#counter');const story=document.createElement('script');story.src='story.js';document.head.appendChild(story);

slides[1].innerHTML=`<p class="kicker">01 · THE PROBLEM SPACE</p><h1>How do we find one useful page in the vast web?</h1><div class="intro-grid"><div><p class="lead">The web contains an enormous number of pages, articles, tutorials, databases, and media files. They are created by different organizations and hosted across many servers around the world.</p><p>When a person needs information, they cannot know where the correct page is stored. The technology that brings this scattered information together is a <b>search engine</b>.</p><p class="callout"><b>Story:</b> You type <code>“how do Python data structures work?”</code>. A search engine discovers likely pages, understands their text, finds relevant candidates, ranks them, and gives you useful links in milliseconds.</p></div><div><div class="placeholder tall"><strong>Image placeholder:</strong><br>“World Wide Web / global connected servers illustration”<br><small>Add an image showing many websites or servers connected through the internet.</small></div><div class="vertical-flow"><div>Billions of scattered web pages</div><i>↓</i><div>User question</div><i>↓</i><div class="highlight">Search engine</div><i>↓</i><div>Ranked links and answers</div></div></div></div><div class="components"><div><b>Crawl</b><span>Collect public pages</span></div><div><b>Parse</b><span>Extract usable text</span></div><div><b>Index</b><span>Organize terms and documents</span></div><div><b>Retrieve & rank</b><span>Return the best results</span></div></div>`;

slides[2].innerHTML=`<p class="kicker">02 · OUR PROJECT</p><h1>What are we building?</h1><div class="intro-grid"><div><p class="lead">We are building a small but real distributed search engine for computer-science and technology documentation.</p><p>The project demonstrates the same essential journey used by large search systems: retain raw web pages, create searchable documents, construct indexes, search multiple partitions in parallel, and merge the most relevant answers.</p><ul><li><b>Offline pipeline:</b> crawler → raw HTML → parser → logical documents → shards → local indexes.</li><li><b>Online pipeline:</b> browser query → coordinator → independent search nodes → global ranked results.</li><li><b>Demonstration objective:</b> show that the system remains useful even when one search node is unavailable.</li></ul></div><div><div class="vertical-flow project-flow"><div>Public documentation pages</div><i>↓</i><div>Controlled ingestion</div><i>↓</i><div>Partitioned local indexes</div><i>↓</i><div class="highlight">Parallel distributed query</div><i>↓</i><div>Search UI + metrics</div></div></div></div><div class="placeholder"><strong>Image placeholder:</strong> “Distributed search engine architecture” — use a professional image showing a browser, coordinator, and several server nodes.</div>`;

slides[3].innerHTML=`<p class="kicker">03 · WHY DISTRIBUTED SYSTEMS?</p><h1>Search at internet scale cannot depend on one machine</h1><div class="stat-strip"><div><strong>8.5+ billion</strong><span>searches estimated per day worldwide</span></div><div><strong>Millions</strong><span>of pages must be indexed and refreshed</span></div><div><strong>Milliseconds</strong><span>expected time to return useful results</span></div></div><div class="intro-grid"><div><h3>What is a distributed system?</h3><p>A distributed system is a collection of independent computers or processes that communicate over a network and cooperate to appear as one service to users.</p><h3>Why it is needed for search</h3><ul><li>Split a large document corpus and index across nodes.</li><li>Search partitions concurrently instead of sequentially.</li><li>Serve results even if one node fails.</li><li>Scale horizontally by adding nodes.</li></ul></div><div class="vertical-flow"><div class="danger">One central machine<br><small>all data · all index · all requests</small></div><i>↓ bottleneck / failure risk</i><div class="highlight">Coordinator + independent nodes</div><i>↓</i><div>Parallel work + partial-failure tolerance</div></div></div><div class="callout"><b>Our design:</b> the coordinator does not contain all postings. Each node owns one document shard and its local inverted index; the coordinator only orchestrates and merges results.</div>`;

const injected=document.createElement('style');injected.textContent=`body{overflow:auto}.slide{height:auto;min-height:100svh}.intro-grid{display:grid;grid-template-columns:1.15fr .85fr;gap:48px;align-items:center}.vertical-flow{display:grid;justify-items:center;gap:9px;text-align:center}.vertical-flow div{width:min(100%,330px);background:#fff;border:1px solid #d9e2f0;border-radius:12px;padding:15px;font-weight:700;box-shadow:0 6px 20px #15223d0a}.vertical-flow i{font-style:normal;color:#07a4a1;font-size:1.5rem;font-weight:800}.vertical-flow .highlight{background:#e5f7f5;border-color:#07a4a1}.vertical-flow .danger{background:#fff0ef;border-color:#d95d58}.components{display:grid;grid-template-columns:repeat(4,1fr);gap:13px;margin-top:30px}.components div{background:#e9f0ff;border-radius:10px;padding:15px}.components b,.components span{display:block}.components span{font-size:.86rem;color:#536987;margin-top:5px}.tall{min-height:160px;display:grid;place-items:center;text-align:center}.stat-strip{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-bottom:30px}.stat-strip div{background:#153e80;color:#fff;padding:18px;border-radius:12px}.stat-strip strong,.stat-strip span{display:block}.stat-strip strong{font-size:1.7rem;color:#8fe9e1}.stat-strip span{font-size:.85rem;margin-top:5px;color:#d9ecff}@media(max-width:800px){.intro-grid,.components,.stat-strip{grid-template-columns:1fr}}`;document.head.appendChild(injected);

let current=0;function show(index){current=(index+slides.length)%slides.length;slides.forEach((slide,i)=>slide.classList.toggle('active',i===current));progress.style.width=`${(current+1)/slides.length*100}%`;counter.textContent=`${current+1} / ${slides.length}`;location.hash=current+1;window.scrollTo({top:0,behavior:'smooth'})}function next(){show(current+1)}function previous(){show(current-1)}document.querySelector('.right').onclick=next;document.querySelector('.left').onclick=previous;document.addEventListener('keydown',event=>{if(['ArrowRight',' ','PageDown'].includes(event.key)){event.preventDefault();next()}if(['ArrowLeft','PageUp'].includes(event.key)){event.preventDefault();previous()}if(event.key==='Home')show(0);if(event.key==='End')show(slides.length-1)});show(Math.max(0,Number(location.hash.slice(1))-1));
